# Kein SSO - Nur IP und Token Auth
# =================================

import snippets/security_baseline.caddy
import snippets/ui_assets.caddy
import extra/global.caddy

# App-Sites werden dynamisch generiert
{{APP_SITE_BLOCKS}}
